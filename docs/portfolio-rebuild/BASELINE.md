# Classic baseline

Baseline `f0f4d77` remains intact. No production source pages changed during visual preproduction.

## Existing paths

`/`, `/finfold`, `/signals`, `/collov`, `/ctrip`, `/unifyux`, `/vw-id-aura`, `/yao`, `/billvampire`, `/weneed`, `/cryptocopilot`, `/pleasurecontract`, `/borrowedlight`, `/writing/`, `/en/writing/`, `/resume/joey-zhao-resume.pdf`.

Homepage anchors: projects, skills, music, writing, contact. Language and resume overlay are current inline JavaScript behaviors. Public site observed in browser on 2026-09-26; desktop 1440×900 and mobile 390×844 screenshots are saved as `artifacts/portfolio-rebuild/baseline-desktop.png` and `baseline-mobile.png`. These are browser viewport captures, not real mobile-device tests.

## Protection

Build new experience under `/explore/`. Root switching to Explore and Classic relocation happen only after visual and release approval. Preserve static article URLs, metadata, sitemap and feed coverage. No push/deploy authorized by visual implementation alone.

## Rollback

Current production is untouched. The isolated branch can be discarded without touching original local edits. For a later approved release, record exact previous deployment ID and commit before switching production; source commit alone is not a verified deployment rollback.
